[package]
name = "flowscope-parser-engine"
version = "0.0.0"
edition = "2021"
description = "FlowScope static analysis engine: project scanner, Java parser, Spring semantic analyzer, business analyzer, graph builder."
license-file = "../../LICENSE"
publish = false
